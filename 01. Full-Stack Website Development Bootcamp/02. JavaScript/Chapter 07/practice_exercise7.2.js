"use strict";

/* Get the full name of your friend. */

/* 1. Using the Person class from Practice exercise 7.1,
      add a method called fullname, which returns the concatenated value
      of firstname and lastname when invoked.
*/

/* class Person {

    constructor(firstname, lastname) {

        this.firstname = firstname;

        this.lastname = lastname;

    };

    fullname(firstname, lastname) {

        return `The full name is ${firstname} ${lastname}.`;

    };

};

*/

/* 2. Create values for person1 and person2
      using the first and last names of the two friends.
*/

/* 3. Using the fullname method within the class,
      return the full name of one or both people.
*/

// let friendName = new Person("Michael", "Jackson");

// let fullname = friendName.fullname("Michael", "Jackson");

// console.log(fullname);

// let anotherFriend = new Person("George", "Michael");

// let fullname = anotherFriend.fullname("George", "Michael");

// console.log(fullname);

