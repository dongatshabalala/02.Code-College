"use strict";

/* 1. Set a variable name and assign a function to it.

      Create a function expression with one parameter
      that outputs a provided argument to the console.
*/

let argument;

// let functionVariable = function (parameter) {

//     argument = "This is a function.";

//     console.log(argument);

// };

// functionVariable(argument);

/* 2. Pass an argument into the function. */

/* 3. Create the same function as a normal function declaration. */

function functionVariable(parameter) {

    argument = "This is a function.";

    console.log(argument);

};

functionVariable(argument);

